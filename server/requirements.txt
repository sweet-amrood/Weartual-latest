# Python deps for server/preprocessing/*.py (Decart image + IRL video).
#
# Installed automatically on `npm install` into server/python_vendor (see package.json postinstall).
# Optional manual: python3 -m pip install -r requirements.txt -t python_vendor
#
# Requires Python 3.10+ (matches `decart`). `realtime` extra is for irl.py (aiortc/av).

decart[realtime]>=0.0.36
